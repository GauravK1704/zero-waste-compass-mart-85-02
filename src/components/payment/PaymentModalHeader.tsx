
import React from 'react';
import { DialogHeader, DialogTitle } from '@/components/ui/dialog';

const PaymentModalHeader: React.FC = () => {
  return (
    <DialogHeader>
      <DialogTitle>Secure Checkout</DialogTitle>
    </DialogHeader>
  );
};

export default PaymentModalHeader;
